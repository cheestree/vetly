import { View, StyleSheet } from "react-native";

export default function ClinicPreviewCard() {
    return(
        <View style={styles.cardContainer}>
            
        </View>
    )
};

const styles = StyleSheet.create({
    cardContainer: {
        
    }
})